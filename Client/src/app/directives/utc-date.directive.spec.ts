import { UtcDateDirective } from './utc-date.directive';

describe('UtcDateDirective', () => {
  it('should create an instance', () => {
    const directive = new UtcDateDirective();
    expect(directive).toBeTruthy();
  });
});
